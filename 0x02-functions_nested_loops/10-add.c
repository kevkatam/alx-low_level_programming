#include "main.h"
/**
 * add - adds two integers and returns the result
 * @i: first number
 * @j: second number
 *
 * Return: addition of the first number and the second number
 */
int add(int i, int j)
{
	return (i + j);
}
